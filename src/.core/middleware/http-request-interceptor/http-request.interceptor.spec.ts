import { HttpRequestInterceptor } from './http-request.interceptor';

describe('HttpRequestInterceptor', () => {
  it('should be defined', () => {
    expect(new HttpRequestInterceptor()).toBeDefined();
  });
});
